[project]
name = "career-intelligence-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
